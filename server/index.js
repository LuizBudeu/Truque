/**
 * Server bootstrap (Phase 3): HTTP server for static client files plus a
 * `ws` WebSocket endpoint. Fully authoritative — incoming ACTION messages are
 * checked with shared/validation.js, applied with shared/reducer.js, and each
 * player receives their filtered view from shared/views.js.
 *
 * No implementation until Phase 3; Phases 1–2 run without a server.
 */

console.error('Server not implemented yet (Phase 3). See docs/PLANNING.md §4.');
process.exit(1);
