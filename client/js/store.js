/**
 * Client-side view store (Phase 2): holds the current PlayerView and the
 * previous one, so animate.js can diff them. Notifies render on change.
 * Views come from the reducer directly (hotseat) or VIEW messages (online).
 */

export {};
