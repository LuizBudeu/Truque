/**
 * WebSocket wrapper (Phase 3): connect, send, auto-reconnect, RESYNC after
 * drop. Speaks the message schemas in server/protocol.js; stores playerToken
 * in sessionStorage so a refresh rejoins the same room.
 */

export {};
